AI_DIR_FILE
tmp_dir                         ./tmp
tmf_dir				./tmf
vbc_dir				./vbc
avc_dir				./avc
allvec_file			./all
pinconfig_file			./s32k144.pin
single_binary_pattern_dir	./single_patterns
 

AI_V2B_OPTIONS  -Tk

PATTERNS name                                               tmf_file            port   v2b_options  
%   subroutines.each do |pattern|
<%= pattern %>                                              s32k144_out.08.tmf  pNVM   -s
%   end
%   patterns.each do |pattern|
<%= pattern %>                                              s32k144_out.08.tmf  pNVM   -T
%   end
%   patterns.each do |pattern|
MULTI_PORT_BURST "<%= "#{pattern}_pset" %>" 
PORTS     pNVM                                  
SEQ_GRPS  grp1                                  
          <%= pattern %>

% end
